# DevOpsLab - wersja z wieloma kontenerami (docker-compose)

## Budowanie i uruchomienie:
```bash
docker compose up -d --build
```

## Wyświetlenie listy aktualnie uruchomionych kontenerów:
```bash
docker compose ps
```

## Testowanie poprawności działania:
http://localhost:3000/
